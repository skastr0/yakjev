# No credentials are needed to build, test, or run this scaffold.
YAKJEV_DATA_DIR=.data
YAKJEV_LISTEN_PORT=3210
# Production: bare HTTPS MagicDNS origin, configured only in Railway.
# YAKJEV_ORIGIN=https://yakjev.example-tailnet.ts.net
# Jev calls are not implemented; never use a VITE_ prefix for provider secrets.
# TYPESAFE_API_KEY=
