{
  "name": "yakjev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "bun@1.4.2",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter '@yakjev/server' --filter '@yakjev/web' dev",
    "build": "bun run --cwd apps/web build",
    "desktop:dev": "bun run --cwd apps/desktop dev",
    "desktop:build": "bun run --cwd apps/desktop build",
    "desktop:start": "bun run --cwd apps/desktop start",
    "desktop:package": "bun run --cwd apps/desktop package",
    "desktop:test": "bun run --cwd apps/desktop test",
    "desktop:smoke": "bun run --cwd apps/desktop smoke",
    "qa": "bun run build && AGENT_BROWSER_SESSION=jev-qa bun tests/acceptance/browser/run.ts --only=J",
    "start": "bun packages/server/src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test packages apps/web",
    "test:deploy": "bash deploy/tests/run.sh && bash deploy/tests/resume.sh",
    "test:acceptance": "bun test tests/acceptance",
    "jev:eval": "bun packages/server/scripts/jev-eval.ts",
    "jev:retrieval": "bun packages/server/scripts/jev-retrieval-eval.ts",
    "format": "prettier --write .",
    "check:format": "prettier --check .",
    "verify": "bun run check:format && bun run typecheck && bun run test && bun run desktop:test && bun run test:acceptance && bun run build && bun run desktop:build && bun run test:deploy"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "prettier": "3.6.2",
    "typescript": "6.0.3"
  },
  "trustedDependencies": [
    "electron"
  ]
}
